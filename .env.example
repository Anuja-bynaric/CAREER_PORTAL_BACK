DATABASE_URL=
PORT=5000
EMAIL_USER=
EMAIL_PASS=

/* The candidate or HR uploads the resume file through the portal.


The backend receives the file and validates its type and size.


The system stores the resume file on the server or cloud storage.


The candidate’s basic details and file path are saved in the database.


The backend sends the resume to the ML resume-parsing module.


The module extracts text from the resume document.


The extracted text is cleaned and prepared for analysis.


The system identifies skills using a model or keyword matching.


A structured list of skills is generated from the resume.


The extracted skills are returned to the backend.


The skills are stored in the database and linked to the user.
ha flow honar jr ml che kont model use kel tr */